import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'نتائج مباشرة',
};

export default function ResultsPage() {
    return (
        <h1>
            Results
        </h1>
    );
};